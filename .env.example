PORT=3055
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5544/magazzino
# Optional fallback only (single-tenant). In production prefer x-workspace-id from user/session.
# WORKSPACE_ID=00000000-0000-4000-8000-000000000999
SEGRETARIA_BASE_URL=http://localhost:3001
# Optional: explicit exchange endpoint for one-click connect.
SEGRETARIA_CONNECT_EXCHANGE_URL=http://localhost:3001/api/integrations/magazzino/connect/exchange
# Fallback manual credentials (used only if no DB connection is configured).
SEGRETARIA_API_KEY=mgk_xxx
SEGRETARIA_HMAC_SECRET=mgs_xxx
